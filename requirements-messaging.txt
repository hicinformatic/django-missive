# Providers Messageries instantanées (Telegram, Signal, Messenger)
# Install avec: pip install -r requirements-messaging.txt

# Dépendances core requises
-r requirements.txt

# Messageries instantanées
python-telegram-bot>=20.7  # Telegram
# Signal nécessite signal-cli-rest-api (service externe Docker)
# Messenger utilise requests (déjà dans requirements.txt)

